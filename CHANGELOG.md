# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed

- **Complete role-specific dashboard recreation.** Completely replaced the shared dashboard composition with three distinct role-specific LMS home experiences for administrators, teachers, and students. Each role now receives a different information architecture, content priority, primary action, and supporting modules optimized for their workflow. Admin dashboard prioritizes organization operations (members, classes, pending invitations) with an operations rail and admin attention queue. Teacher dashboard features a dominant teaching queue (submissions needing grading, unread student questions, overdue work alerts), teacher pulse metrics with zero-value suppression, upcoming classwork deadlines, students requiring check-in, and teaching-focused activity. Student dashboard centers on a "next learning action" panel showing the most relevant unfinished item or recently graded work, attention queue with due-soon work and messages, learning runway timeline grouped by buckets (today, this week, later, completed), feedback & messages section, and learning-focused activity. Admin role takes absolute priority when the user has organization owner/admin permissions. All dashboards use the established UpClass Classroom Focus visual direction: near-white canvas, white working surfaces, semantic blue for actions, course colors for identity only, Geist typography, restrained elevation, 12px panel radius, no gradients or decorative elements. Each role has a memorable signature "today board": admin operations rail, teacher teaching queue, student next action panel. Dashboard data layer completely refactored with typed role-specific view models (`AdminDashboardViewModel`, `TeacherDashboardViewModel`, `StudentDashboardViewModel`) built server-side with parallel query fetching and proper empty state handling. New role-specific skeletons (`AdminDashboardSkeleton`, `TeacherDashboardSkeleton`, `StudentDashboardSkeleton`) match loaded layout geometry to prevent layout shift. Responsive behavior: mobile uses single-column priority stack with touch-safe actions, desktop uses wider containers with teacher/student primary-rail layouts (dominant queue left, supporting rail right). Created comprehensive dashboard primitive library: `DashboardShell`, `DashboardSection`, `DashboardGrid`, `DashboardHeader`, `DashboardMetricStrip`, `DashboardEmptyState`, `DashboardNextAction`, `DashboardAttentionQueue`, `DashboardDeadlines`, `DashboardClassList`, `DashboardActivity`, `DashboardAiCue`. All components support localized Suspense boundaries, preserve existing authentication/authorization/routing/caching/offline/realtime behavior, and render only stored data (no invented attendance, schedules, rankings, engagement percentages). AI remains contextual and secondary with role-aware briefing prompts. Added comprehensive test suites covering role-specific rendering, empty states, metrics visibility, primary action selection, queue ordering, and formatter utilities. Updated `DESIGN.md` with dashboard architecture documentation, `components/ui/design-system.md` with dashboard composition rules, and `components/ui/QUICK_REFERENCE.md` with role-specific dashboard examples.

### Added

- **Structured class and resource creation with grade levels, sections, and types.** Classes now have explicit grade level (K-12, college, or custom), optional section names, replacing the generic text category field. Resources feature structured types (lecture notes, assignment, reading material, exam review, lab manual, syllabus, other) and optional class linkage with visible class context in resource views. Database migration (0017) adds `grade_level`, `custom_grade`, and `section` columns to `classes`, and `resource_type`, `class_id` (nullable FK with ON DELETE SET NULL), and `storage_path` to `resources`. Class and resource forms display the new fields with conditional validation (custom grade required when grade level is "other"), and class identity now formats as "Title - Grade - Section" via `formatClassIdentity` utility. Resource creation remains online-only (excluded from offline queue due to file uploads), resource deletion includes Supabase storage cleanup, and activity logs show structured metadata. Updated all class/resource queries, display components, validation schemas, and tests to use the new fields while preserving legacy `category` columns for backward compatibility during migration.

- **Unified AI sidebar with resource context and grounded recommendations.** The AI assistant panel now supports resource-surface conversations with full resource text injection, optimized lazy-loading lifecycle (instant new chat, explicit history loading), and server-generated grounded recommendations. Extended database schema to include `resource` surface in `ai_conversations`, migrated existing `resource_ai_conversations` to unified tables, added resource authorization in `resolveAiSurfaceAccess`, and updated orchestrator to inject resource context as untrusted quoted content. The panel opens instantly with a temporary conversation ID that persists in the background, history loads only on explicit History button click, and recommendations are generated deterministically from tool results and structured cards with validation against available tools. Resource pages now open the unified sidebar with resource context via `useAiPanel().openFor()` instead of the deprecated `AIChatDialog`. Added recommendation system with types, generator, validator, and index module supporting intent-based filtering, tool capability validation, and grounding in actual assistant responses. Hard-coded suggestion arrays replaced with metadata-driven recommendations.

- **Two-layer onboarding.** Account setup only asks for display name when missing; `/org` stays workspace hub. Teachers get resumable wizard at `/org/create` (workspace → first class → enrollment invite → People tab). Students join via `/org/join?code=` and land in class directly. Added `onboarding_state` table, org roles `teacher`/`student` with migration backfill, class code enable/disable/regenerate, atomic `joinOrganizationAndClassByCode`, permission helpers, analytics events, People-tab setup checklist, and `/onboard` redirect.

- Added an idempotent demo-data seeder with four classes and eight dummy students for `definitelynotmark13@gmail.com`.

- **Messaging reliability and scale foundation.** Added organization-scoped direct conversations, cursor pagination endpoints, idempotent client message IDs, denormalized thread summaries, per-thread read cursors, offline text queue metadata, notification outbox processing, and Supabase JWT/RLS protection. Ambiguous legacy direct rows are preserved in a migration quarantine table; Redis remains reserved for future ephemeral presence, typing, and rate-limit state.

- **Model-free AI pipeline eval suite.** Added a scored eval harness (`tests/evals/ai/`) that runs the safety and plumbing invariants of the AI pipeline against the real modules in CI — tool output truncation keeps JSON parseable, action proposal parsing is exact, the output filter contains canary/fragment leakage, memory scoring ranks sensibly, and the pulse briefing prompt stays compact. The evals surfaced and fixed two hardening gaps: `parseActionProposals` no longer swallows a valid proposal when stray text precedes it (segment-based parsing now uses the last opener before the closing tag), and the role-revelation leak pattern now catches the full form "I am an AI language model trained by …".

- **AI trust & observability hardening.** Every AI turn (assistant, resource chat, quiz generation, pulse briefing) now carries a per-turn `runId` that flows through usage logs, message metadata, tool events, action confirmations, and feedback, and all usage logging reports real token counts from the model finish callback instead of estimates. Duplicate assistant/resource-chat requests are rejected with a 409 before the model is ever called, and a long-standing bug that sent the just-persisted user message to the model twice (or not at all on replayed turns) is fixed — the stored row is dropped from the model view and the current sanitized message appended exactly once. Quality-gated turns record an uncertainty warning on the message when the post-sanitization output fails the canary check. Action confirmations are now idempotent: each proposal carries a server-issued `proposalId` recorded in a new `aiActionProposals` ledger, so re-confirming replays the stored result and never double-executes. The provider policy layer classifies every surface (`dashboard`/`class`/`resource`/`quiz`/`pulse`) into sensitivity tiers that constrain which models may handle what data, and the prompt pipeline was reworked into a static cached core (LRU, per-surface key) with per-request dynamic context (memories, summary, canary) appended outside the cache. New kill switches (`AI_DISABLE_*`) can turn off resource chat, quiz generation, and the pulse independently. Resource chat now authorizes via owner-or-shared-org membership and answers 404 for both missing and forbidden resources so existence is never leaked.

- **Daily Class Pulse.** The dashboard shows a role-aware daily pulse card (teachers and students): aggregate facts — ungraded submissions, upcoming work, class activity, attendance pulse, and nearest deadline — fetched from a cached (`pulse:facts:{userId}`, 5-minute TTL) facts endpoint, with a "Brief me" action that streams a short 60–110 word briefing from the assistant (student-linked sensitivity) via a new `PULSE_BRIEFING_MAX_TOKENS`-capped endpoint. Admins never see the card.

- **Assistant feedback.** Assistant messages on the chat page and side panel gain thumbs up/down controls that persist ratings (with optional reason) to a new `ai_feedback` table via `POST /api/ai/feedback`, keyed by message and user with upsert semantics.

- **Completed the UpClass product-wide Classroom Focus redesign.** Replaced the Warm Paper/Poppins direction with Geist typography, neutral classroom surfaces, semantic blue interactions, restrained elevation, consistent Lucide navigation, and responsive shell primitives. Rebuilt the role-aware dashboard around today's work and actionable workload, refined class collection surfaces, removed decorative landing/onboarding treatments, and aligned auth, public, branded image, email, and PWA surfaces. Existing tenant routing, permissions, server actions, offline/realtime behavior, AI workflows, and data contracts remain intact. Verified with TypeScript, ESLint (no errors), 280 Vitest tests, production build, and static visual-token audit.

- **Shadcn sidebar and Poppins typography refresh.** Migrated the authenticated navigation shell to the shadcn Sidebar provider, icon rail, menu groups, tooltips, and mobile Sheet while preserving tenant-aware routes, recent classes, organization switching, realtime unread counts, and profile/admin access. Desktop collapse state now persists through the standard sidebar cookie without a hydration flash, the white navigation surface has 12px top-right and bottom-right corners, and Poppins is the global sans/display family across the app.

- **Home visual reconstruction.** Rebuilt the dashboard composition around a high-contrast priority workspace, a single embedded AI action band, direct next-item navigation, compact class workspace rows, hidden zero-alert teacher context, and unified lower activity surfaces. Removed the visible role badge/circle treatment and repeated icon-tile framing while preserving existing queue, class, activity, AI, and tenant-aware behaviors.

- **Polished Classes workspace.** Refined the Classes page into a consistent app surface with a clearer search/sort/view rail, stronger course identity posters, role-aware class badges, aligned roster metadata, explicit entry affordances, responsive grid/list sizing, and a quieter empty state while preserving create/join flows and tenant-aware navigation.

- **Studio-focused `/home` redesign.** Redesigned the `/home` dashboard (`app/[orgSlug]/(main)/dashboard/`) around high-urgency action and calm studio aesthetics:
  - **Orientation Header**: Features dynamic time/greeting context, concise role description, status badge, and a role-aware primary action button (`Review submissions` for teachers, `View due work` for students).
  - **Unified Time-Sensitive Action Queue (`HomeActionQueue`)**: Replaces crowded top stat tiles with an urgency-prioritized queue showcasing overdue assignments, tasks due today/soon, pending submissions needing grading, overdue alerts, and unread questions with semantic status badges and tenant-aware links.
  - **Contextual AI Cue (`HomeAiCue`)**: Subordinate suggestion card that derives truthful prompt recommendations from visible queue tasks and seamlessly connects to the persistent sidebar AI assistant with class seed context.
  - **Restrained Active Classes Workspace**: Streamlined course cards with course identity swatches, member counts, and direct Stream/Work/Board sub-links without heavy redundant card borders.
  - **Secondary Context**: Relocated weekly analytics and participation alerts below the fold for teachers, alongside the learning progress pulse and recent activity log.
  - **Updated Skeletons & Test Suite**: Updated `DashboardOverviewSkeleton` and added unit test coverage for the queue, AI cue, and orientation header actions.

- **Class cards that show the class.** Rebuilt the Classes collection card around what a teacher or student actually needs to see. The card is a poster: a full-bleed course-tone cover carrying the class monogram (plus a quiet join-code chip for teachers), a larger `type-h2` title, description, and quiet metadata chips for schedule, classwork, and student count — `No students yet` is stated when the roster is empty, and the redundant category chip is gone. The footer always shows the teacher-first avatar stack with a truthful `+N` overflow beside a relative "updated" stamp, so new classes never look broken. Elevation is declared once (`shadow-e1` in every state) with no ring stacked on top and no hover lift — the only hover cue is the title shifting to `text-primary-strong` — and list view is a genuine full-width horizontal row. The classes query now returns each class's last-changed time, classwork count, and first student avatars in the same round trip (no per-card fetching), the grid fills ultrawide displays through a width-driven `auto-fill` track, the search toolbar uses one shared 40px control height with borderless filled search/sort controls and a neutral result count, and skeletons mirror the new anatomy. Added shared `AvatarGroup` and `formatRelativeTime` helpers plus a `layout.full` page width for reuse.

- **Reference-aesthetic alignment pass.** Aligned shared controls and the two signature surfaces to the reference treatments. Segmented controls (`Tabs` `pill`/`solid`, used by the Classes Grid/List toggle, settings, and org admin) now render an azure-filled active segment on the gray track instead of a white card, with the previous per-instance class overrides removed. The chat composer was rebuilt as a floating white rounded card with soft shadow: borderless auto-growing textarea, gray ghost attach/history controls (styled to match, marked coming soon), an "Auto" model chip, and a circular azure send button that becomes the stop control while streaming. The whiteboard gained a floating icon-only canvas toolbar — azure-filled active tool, gray ghost inactive tools, a functional zoom stepper (−/%/+, mirrored from pinch and Ctrl+wheel zoom) and a JSON export button — kept in sync with Excalidraw's own tool state; the save status stays in the page header. Buttons gained an `icon-circle` size, the classes search bar dropped its double-edge ring/shadow for the clean hairline input, and resource file-type tiles now use semantic tone surfaces instead of hard-coded hex. Course color pickers, presence colors, and Excalidraw canvas config remain data/config values untouched.

- **Tinted secondary buttons.** Secondary buttons now follow the reference design: borderless pale-azure fill with deep azure text instead of the previous gray fill. The shared tint token deepened from `#DFF2FF` to the reference `#CEEBFF`, so selected nav pills, sidebar accents, and tinted badges pick up the same stronger azure tint while keeping WCAG AA text contrast (`#0768B0` on `#CEEBFF` ≈ 4.7:1). For consistency, the `outline` variant now renders identically to `secondary` (the white bordered outline button is retired), which converts every secondary action app-wide in one move — the Google continue button, "Join class", dialogs, toolbars, and landing/auth CTAs — while `ghost`, `link`, `inverse`, and `destructive` keep their distinct roles.

- **Collapsible icon-rail sidebar.** The desktop sidebar can now collapse into a 64px icon rail instead of disappearing, mirroring the assistant panel pattern. A header toggle (`PanelLeftClose`/`PanelLeftOpen` next to the Ask AI button) animates the width between the full rail and icon mode with the same easing family; collapsed state persists per browser via localStorage. In rail mode the brand mark, org avatar, search, navigation, recent-class swatches, and profile avatar stay interactive as centered icons with `title` tooltips and accessible names, the unread count hides until expanded, and the phone drawer is unaffected (labels always show below `md`).

- **Gray Canvas redesign with azure primary.** Re-themed the app from Sky Studio to a gray-canvas language matched to the new reference: `#EBEBEB` workspace, pure-white sidebar and cards, and an azure primary (`#0B99FF` fill with white text, `#0878CC` strong links/icons, `#DFF2FF` tinted surfaces with `#0768B0` text). Cards, panels, and AI data cards dropped their hairline borders for borderless white sheets resting on a softer `shadow-e1` (with `shadow-e2` hover lifts), the sidebar is now white with an `#E2E2E2` divider and rounded `8px` nav pills, buttons follow the reference set (solid azure primary, borderless azure-tint soft variant, white outline buttons with `#E4E4E4` borders), neutral grays replaced blue-tinted hairlines/muted text (`#E4E4E4` / `#737373`), charts moved to an azure ramp, and dark mode received matching neutral `#121212` surfaces with azure accents. Social images, theme-color meta, and design docs were updated to match.

- **Non-blocking route shells with granular skeletons.** Removed every route-level `loading.tsx` under `app/[orgSlug]/(main)/` and retired the `InstantLink`/`startTransition` navigation layer in favor of plain `next/link`. Each authenticated route now returns its static shell synchronously and streams data regions through localized `<Suspense>` boundaries with matching skeletons, so clicks commit the new route immediately (no keep-old-page overlay, no full-page skeleton swap) while only data-dependent regions show loading state. Navigation-layer components (`instant-link`, `instant-navigation-provider`, `use-instant-navigation`, `lib/instant-navigation`, `route-content-transition`) were deleted and their consumers migrated to framework-native `<Link>`.

- **Inter typography with graphite ink.** Moved the whole app to a single Inter variable family (replacing the Geist Sans + Bricolage Grotesque pairing; Geist Mono stays for code), retuned the type scale to a semibold (600) tight-tracked heading hierarchy over regular (400) body copy with normal tracking and medium (500) labels, and shifted the text token ramp from navy ink to neutral graphite — `#111111` foreground, `#333333` secondary, `#6B7280` muted in light mode with neutral counterparts in dark mode.

- **Instant per-route static shells.** Route navigation no longer shows a keep-old-page overlay or fades to a generic fallback. Each authenticated route now paints its real, static shell (actual page title, description, and toolbar) immediately on click, with skeletons limited to data-dependent regions. Route content swaps in a single committed render once the payload streams in, and heavy pages (classes, resources, settings, admin, profile) stream their data via server-passed promises resolved client-side, so the shell renders before the data arrives.

- **AI chat hybrid surface: right panel + full-page focus mode.** The assistant panel is now a real chat surface sharing one thread component with the full-page chat. A Sparkles toggle in the header (all breakpoints, `Ctrl+J`) opens a right-side drawer that pushes content on desktop (`--app-ai-panel-width`) and overlays with a scrim, Escape, scroll lock, and focus management on mobile. The panel supports an inline launcher (context chip when seeded from a class, inline new chat, recent conversations with confirmed delete, admin knowledge base), and an inline thread with streaming, structured cards, and action confirm cards; Expand deep-links to the same conversation at `/chat/{id}`. Class detail pages gain an "Ask AI" header entry that seeds the panel with the class's default conversation (with a "Start fresh" reset). Panel state survives toggling and route changes.

- **Org-scoped AI assistant.** Added a tenant-aware assistant (chat page at `[orgSlug]/chat`, slide-in panel via `Ctrl+J`, class surface conversations) built on the Vercel AI SDK. Orchestration covers intent classification with caching, embedding-based retrieval over an admin-curated org knowledge base, conversation compression with persisted summaries, per-intent token budgets, and a 9-module prompt budgeter with a per-org HMAC canary token. 25 read tools return text plus structured data cards; 4 draft action tools (announcements, assignments, quizzes, channel messages) surface interactive confirmation cards that execute only on confirm, teacher-gated end to end. Safety includes input sanitization, output canary/fragment redaction, per-user rate limits with persisted failed turns, monthly org budgets, and per-turn step/tool caps. Conversations and messages persist in new `ai_*` tables (migration 0010), and a daily cron (`CRON_SECRET`-protected) prunes token logs after 90 days.

- **Migrated AI implementation to Vercel AI SDK.** Replaced custom Cerebras API client with Vercel AI SDK's OpenAI-compatible provider. The resource AI chat now uses `streamText`, `useChat`, and built-in streaming primitives, eliminating ~600 lines of manual SSE parsing, state management, and error handling while maintaining all existing features including conversation persistence, rate limiting, and text extraction. The same Cerebras API key and model configuration is used via the AI SDK's standardized interface.

- **State management refactor: server data out of Zustand.** Classes, resources, direct/channel messages, notifications, and profile settings no longer live in client stores. Server Components remain the source of truth and stream fresh props on navigation; realtime events, optimistic sends, and offline hydration now write into component-local state. Client fetching was consolidated on TanStack Query for the organization switcher, user lookup in new conversations, and AI quiz generation, with a persisted `currentOrg` store and a tiny client-only unread-badge store remaining. All offline/IndexedDB caching, background sync, and realtime behavior is preserved, and six now-dead stores were removed.

- **Sky Studio classroom-app UI redesign.** Completed the app-wide redesign replacing previous mint/obsidian styling with a cohesive Sky Studio visual language: cool paper canvas (`#F3F7FB`), navy ink typography (`#142033`), and a high-contrast light-blue primary palette (`#C5E2F6` fill, `#0C4A6E` text, `#1780B8` strong). Surfaces are flat at rest with elevation strictly reserved for floating sheets and overlays. Typography pairs Geist UI with Bricolage Grotesque for titles, with clean semantic tokens across landing, auth, onboarding, home overview, class workspaces, classwork workflows, quiz taking and review, direct/channel messaging, resource hub, activity feeds, notifications, user profiles, settings, and whiteboard chrome while preserving all underlying data contracts, realtime sync, and offline PWA capabilities.

- **Light-blue primary theme & button primitive enhancements.** Transitioned the primary brand color system across light and dark modes from mint green to a modern, high-contrast light blue sky palette (`#e0f2fe`, `#0284c7`, `#0369a1`). Upgraded the shadcn `Button` primitive with tactile active micro-animations (`active:scale-[0.985]`), refined elevation and border transitions, loading state handling, and optional `leftIcon`, `rightIcon`, and `fullWidth` props.

- **Mint-green design system migration.** Replaced the blue-accented Compact Learning Studio palette with a calm mint-green token layer across light and dark themes: pale mint primary actions (`#D7F4EA`), strong green text accents (`#0E6B52`), updated sidebar active states, chart palette, focus/selection rings, authenticated shell gradient, elevation ramp, and geometry tokens (wider sidebar, 16px container radius, 40px default control height). Hard-coded blue defaults in profiles, classes, OG images, and whiteboard presence were migrated; course identity colours were preserved where appropriate.

- **Learning-index sidebar polish.** Clarified the single real-data sidebar with labeled Workspace, Recent classes, and Manage groups; mint active markers and icon tiles; readable 13px navigation rows; larger mobile targets; and cleaner organization/profile cards while retaining tenant-aware routes, realtime counts, recent-class data, and drawer accessibility.
- **Distinctive Classes workspace.** Moved Join class and Create class from the global top bar into an editorial in-page header, removed the Teaching/Enrolled split and repeated card labels, combined both real datasets into one searchable collection, and aligned the page to the shell margins. Search, sort, Join, and course-card bodies remain white and use one neutral semantic edge plus controlled elevation for visibility; blue is reserved for Create, focus, selected view, course artwork, and meaningful metadata. The collection also includes a real horizontal list view and matching empty/loading states without generic nested dashboard framing.


- **Fullscreen, data-backed application shell.** Removed the inset page frame, outer gray gutter, secondary icon rail, aggregate section tabs, generic content actions, collaborator placeholder, artificial content categories, and promotional sidebar card. The application now fills the viewport and uses one sidebar with only live tenant routes, realtime counts, recent classes, organization tools, settings, and profile data.
- **Light-blue Geist typography refresh.** Replaced Inter with optimized Geist Sans and Geist Mono, moved primary actions, selections, focus, and sidebar states from indigo to a light-blue hierarchy in both themes, and raised the compact type scale for more readable body copy, metadata, controls, cards, and headings without losing density.
- **Compact Learning Studio reference redesign.** Reframed the product around a blue accent, readable Geist typography, dense controls, and shallow illustrated class/resource cards. Public, authentication, onboarding, settings, administration, and whiteboard chrome share the same visual language while tenant routing, truthful data, accessibility, PWA/offline caches, realtime behavior, and server contracts remain intact.

- **Prior Ink & Riso redesign (superseded visual direction).** The earlier paper-neutral work introduced the Time Rail, action-first Home content, semantic feedback, command navigation, and shared behavior that remain in place; its magenta palette, course-spine rows, and visual styling have been replaced by the Compact Learning Studio system.

- **Resource-grounded Cerebras AI chat.** The resource assistant now uses Cerebras chat completions, reads PDF, DOCX, XLSX, CSV, and plain-text uploads server-side, and only accepts resource IDs from the browser so answers are grounded in authoritative resource content.
- **Persistent private resource AI conversations.** Resource chat now restores each user’s previous messages for that resource, saves successful exchanges transactionally, and uses the saved conversation as server-side context for subsequent turns.
- **AI-assisted quiz generation.** Teachers can generate editable class quiz drafts with Cerebras from their own instructions and up to five uploaded PDF, DOCX, XLSX, CSV, or text references.
- **Streaming resource AI chat.** Assistant replies now stream token-by-token into the conversation, with a 55-second upstream timeout, automatic retry on rate-limit responses, and clear error states instead of a silent failure.
- **Safer, more scalable resource AI chat.** Extracted source text is cached per resource, chat history is capped and pruned at 60 messages, duplicate sends are prevented per user turn, and both chat and quiz generation are rate-limited daily and per minute; assistant markdown links are restricted to `http`/`https` so unsafe schemes render as plain text.
- Resource AI chat and quiz generation now surface readable errors when a resource file cannot be retrieved or parsed (network failures and PDF/DOCX/XLSX parse errors) instead of a generic "Failed to get AI response".

- **Instant navigation with immediate skeleton display.** Clicking any internal link now swaps the content area to the destination route's skeleton at 0ms while Next.js streams the Server Component payload in the background, so navigation feels instant instead of holding the previous page. The sidebar and mobile dock highlight the destination immediately on click, hover/focus prefetching warms every primary route, opening the mobile drawer prefetches its destinations, and per-route loading boundaries stream the same skeletons on full page loads.

- **Navigation streams pages instead of blanking them.** Clicking a link no longer swaps the whole content area for a full-page skeleton: the current page stays mounted until the new route's payload commits, and each page streams its own per-section loading states (`loading.tsx`/Suspense) for only the parts that are still resolving. Back/forward, refresh, and search-only changes keep content mounted the same way, so visited routes render instantly from the router cache and unvisited routes show their real page shell immediately. Sidebar and mobile-dock highlighting still reacts at 0ms from the pending destination. Realtime-triggered `router.refresh()` calls are now coalesced per channel (trailing 1s window) so bursts of events no longer wipe the router cache repeatedly, keeping visited pages instant between refreshes.

- **Navigation polish: sidebar highlights follow in-flight navigation; visited routes stay cached in dev.** The Messages and Notifications sidebar rows now light up from the pending destination at click time (like the primary items) instead of waiting for the URL to commit, so no row lingers on the previous section while a route streams in. `cachedNavigations` and `staleTimes` are now enabled in development as well as production, so returning to a visited page renders instantly from the router cache while developing instead of refetching on every back/forward navigation.

- **Navigation polish: no flash-back, no skeleton on back.** The pending skeleton is now dismissed only when the destination route's content actually commits — not when the URL catches up mid-fetch — so the previous page can never flash back between the skeleton and the committed route. Back/forward navigation and search-parameter changes keep the current content mounted until the router cache or stream resolves the payload, so returning to a visited page never falls back to a loading skeleton; visited routes render instantly from the router cache in production (`cachedNavigations` + `staleTimes`). The sidebar also highlights exactly one item on class pages: the specific recent-class row wins over the "Classes" section, which takes over only for class pages not in the recents list.

- **Complete per-route skeleton coverage.** Activity, profile/user, resource detail, conversation and class-channel messages, whiteboard, quiz taking, and admin routes now each have a dedicated skeleton matching their real page structure, delivered both by streaming `loading.tsx` boundaries and by the instant-navigation fallback on client-side clicks. The hand-built admin loading markup was extracted into a shared `AdminPageSkeleton`.

- **Scoped server revalidation.** Server actions no longer call org-less `revalidatePath()` (which could not match tenant routes). Revalidation is now resolved from records and sessions via `lib/server/revalidate.ts`: class actions revalidate through `classes.orgId → orgs.slug`, user-scoped actions revalidate every org the user belongs to, so class pages, resources, messages, notifications, settings, and profile changes refresh the correct `/[orgSlug]/...` paths.

- **Queried data freshness tuning.** The organization switcher list is cached 5 minutes (30-minute garbage collection) and is prefetched when the mobile sidebar drawer opens so the switcher is instant; user lookup in the new-conversation dialog caches per email for 1 minute; the whiteboard no longer refetches the snapshot API on every mount since the server page already streams the freshest version (conflict reload still refetches); and the class quizzes tab now loads questions and attempts in parallel instead of serially.



### Added
- **Global design theme.** A reusable three-layer system so every surface can share one design language: semantic tokens in `app/globals.css` (status tones with soft/solid/border/text variants, layered surfaces, modal overlay, a four-step elevation ramp, shared easing curves, a fluid typography scale, and interaction utilities), typed variants and layout rhythm in `lib/design-system.ts`, and shared primitives in `components/ui/*`.
- **Editorial landing page redesign.** Rebuilt the public homepage around a Notion-inspired product story with a workspace preview, clearer feature chapters, a more intentional conversion flow, and a motion-safe animated color and underline treatment for the hero’s second tagline line.

- New shared primitives: `Text`/`Eyebrow`/`Mono`, `Panel`, `PageContainer`/`PageHeading`/`SectionHeader`, `IconBadge`, `StatusBadge`, `StatGroup`/`StatTile`, the `DataTable` family, the `Field` family with `InputAffix`, `Callout`, `EmptyState`, `EntityAvatar`, `CourseIdentity`/`CourseSwatch`, `EntityRow`, `FilterToolbar`, `TimelineRow`, `ResponsiveSplitView`, and `CopyButton`.
- `Tabs` now ships `line`, `pill` and `solid` variants so section navigation is consistent across features.
- Organization admin console gained a pending-invitations view, backed by new `listOrganizationInvitations` and `revokeInvitation` server actions.
- Shared organization domain types in `types/organization.ts`, including role labels, descriptions and tones, replacing three drifting inline copies.
- Reserved organization slugs are now rejected at validation time so a workspace URL can never shadow a real top-level route.
- Streaming skeletons for the `/org` routes and the organization console, replacing centred spinners.
- Universal design system documentation (`components/ui/design-system.md`) and a copy-paste companion (`components/ui/QUICK_REFERENCE.md`).
- Minimal public `/terms`, `/privacy`, and `/contact` pages so auth and support links resolve to live routes.
- Draft/resubmission assignment workflow support with multiple submission attachments, revision history, grading history, and richer classwork review dialogs.
- Teacher analytics on Home covering overdue work, a global review queue, unread student questions, low-participation alerts, and weekly summary cards.
- Class `general` channels, unified messaging search, and offline queue support for direct and channel message sends.
- **Figma UI3-inspired design-system foundation.** Rebuilt application chrome around primitive cool-neutral and blue ramps aliased through light/dark semantic tokens, an explicit 4px spacing grid, density-aware control heights, semantic control/container/floating radii, Inter variable typography, and tokenized motion durations. High-impact shadcn primitives now share compact geometry, precise hover/pressed/focus states, and consistent floating surfaces while preserving LMS course identity and accessible mobile targets.
- **Modern LMS card system.** Shared cards provide structured action/content/footer slots and semantic flat, raised, interactive, inset, and workspace surfaces. Class tiles now use soft semantic course-color heroes, compact role and academic metadata, a restrained course glyph instead of character artwork, truthful schedule and enrollment details, and a separate teacher/action footer with matching streamed skeletons.

- **Unified role-aware dashboard.** Home and Activity are now one `/[orgSlug]/dashboard` route: greeting, live work metrics, role-specific quick actions, the teacher review queue (with "Grade submissions" jumping straight to it), the time rail, upcoming deadlines, and the activity history. `/home`, `/activity`, and the org root all redirect into the dashboard, and `/activity` deep-links to its activity section.
- **Calendar route.** A new `/[orgSlug]/calendar` page groups coursework due in the next 30 days by day (Today/Tomorrow/date headers) with submission states for students, plus each class's recurring schedule alongside.
- **Header notification bell.** Unread notifications moved from the sidebar into the page header: live unread badge, realtime updates, a quick preview of the latest five notifications with class links, and mark-all-read. The full timeline remains at `/notifications`.
- **Keyboard shortcuts.** `g d/c/m/r` navigate to the dashboard, classes, messages, or resources; `c a/m` start creating an assignment or message; `/` opens the command menu. Typing in fields is never intercepted.
- **Class-page breadcrumbs.** Class detail pages now surface the class name in the header breadcrumb via a route-level title setter.
- **Command menu upgrades.** The command palette gained Calendar and Notifications destinations, quick actions for creating assignments and messages, and visible shortcut hints for every entry.



### Changed
- **Warm, paper-neutral design foundation (Notion-calm pass).** Migrated the light and dark neutral palette from cool blue-gray to a warm, low-chroma paper tone (surfaces, `foreground`, `muted-foreground`, hairlines, sidebar, borders), softened the primary brand blue, warmed and flattened the `shadow-e1`…`shadow-e4` elevation ramp, and pointed the focus ring at the brand accent — all through the existing `app/globals.css` tokens with no new token names or Tailwind config. Every semantic tone, status, and course-identity color is preserved and continues to meet AA contrast in both themes.
- **Reference-led LMS home workspace.** Reorganized authenticated Home into a dense, responsive learning dashboard with a compact greeting, five tenant-aware action shortcuts, four live work metrics, a course overview, role-specific teacher attention queues, a sticky upcoming-work rail, and activity history. The layout only presents data UpClass actually stores—no invented rankings, completion percentages, or upgrade sales content.
- **Learning-studio application overhaul.** Rebuilt the authenticated product language across the canvas, shell, floating top chrome, sidebar and course shelf, mobile dock, page headers, course workspaces, dashboards, collection toolbars, split messaging, settings, data surfaces, and loading geometry. Shared panels, cards, metrics, buttons, inputs, selects, tabs, badges, switches, menus, dialogs, tables, and overlays now use layered borderless elevation, larger semantic object radii, inverse primary actions, colorful icon tiles, and finite course identity so UpClass reads as a cohesive LMS application rather than generic SaaS panels. Tenant routing, role behavior, realtime subscriptions, offline support, focus management, and existing data contracts are unchanged.

- **Expanded shared Button system.** Buttons now provide polished `soft` and `inverse` treatments, dense and icon size tiers, stable no-shift loading labels, explicit variant/size state hooks, accessible busy state, and consistent elevation, hover, pressed, invalid, focus, and disabled feedback across every variant.

- **Borderless action and course surfaces.** Shared buttons now use semantic fill and elevation for hierarchy instead of permanent outlines, with clearer hover and pressed feedback; white `outline` actions retain their API while rendering as raised borderless surfaces. Class cards likewise drop their outer frame and footer divider so course color, shape, and shadow define the tile.
- **Calmer home and class surfaces.** Flattened the home dashboard from floating raised cards to hairline-separated panels, replaced the greeting hero (gradient, dotted texture, oversized watermark) with a quiet editorial header, and replaced grid-wide hover-lift with subtle neutral hover on the home and class-list tiles. The class detail header no longer floats and now carries a narrow course-identity edge instead of a generic brand edge, so course color reads as identity rather than decoration.
- **Organization onboarding redesign.** `/org`, `/org/create` and `/org/join` now use a calm classroom-entry flow: existing organizations appear in a compact linear directory for faster scanning, create/join choices live in a distinct notebook-style action rail, and focused forms retain slug validation, URL preview, and invite guidance without decorative gradient treatment.
- **Organization console redesign.** `/[orgSlug]/admin` moved from three stacked blocks to a proper console: organization identity header with monogram and copyable workspace link, a metric cluster, segmented People/Classes/Invitations tabs, Linear-style responsive tables, a Stripe-style invite composer using the `Select` primitive instead of native `<select>`, and confirmation dialogs for destructive actions.
- Organization role and membership changes now revalidate the affected routes and refresh in place, instead of asking the user to reload the page.
- Organization server actions validate through Zod schemas in `lib/validation/actions.ts` rather than hand-rolled regex checks, and `createInvitation` now rejects addresses that already belong to the organization.
- The sidebar workspace switcher was rebuilt with a monogram, role subtitle and loading skeleton.
- `listUserOrganizations` computes member counts with a single grouped aggregate instead of one query per organization.
- Motion across the app now honours `prefers-reduced-motion`.
- **Repository-wide LMS redesign.** Rebuilt the public, authentication, onboarding, dashboard, classes, course, quiz, resources, messaging, activity, notifications, profile, settings, user, and organization administration surfaces around compact semantic panels, rows, timelines, truthful states, and shared design-system primitives in light and dark themes.
- Classes and Resources collections now use responsive semantic cards with matching loading skeletons while retaining filtering, offline hydration, tenant-aware navigation, prefetching, and file downloads.
- **Adaptive LMS tenant shell.** Desktop/tablet navigation now uses an organization-aware sidebar structured around Learning, a prominent recent-course shelf, Inbox, and Organization tasks, with stronger active states and 44px targets; phones retain safe-area-aware bottom navigation with a keyboard-accessible More drawer. Role-aware destinations, skip navigation, focus restoration, realtime unread badges, and tenant-preserving links remain consistent across routes.
- **Streamlined navigation.** The sidebar workspace now offers Dashboard, Classes, Messages, and Resources, with Settings and profile in Manage; the mobile dock shows Dashboard, Classes, Messages, and More. Notification entry points, org-switcher and onboarding destinations, the PWA start URL, and service-worker core routes all point at the dashboard.
- **Collaborative whiteboard chrome.** The Excalidraw workspace now has compact class navigation, accessible collaborator and save/upload/conflict status, semantic responsive framing, and light/dark canvas controls while retaining optimistic persistence, realtime collaboration, conflict reloads, and offline blocking.
- Loading, empty, error, not-found, offline, PWA update/install, dialog, and navigation-progress surfaces now share semantic tones, elevation, dynamic viewport behavior, and focus-managed overlays without gradients, glow, or glass effects.
- Shared surfaces now use clearer layered elevation, dashboard overview framing, course workspace headers, semantic course/file identity bands, and tangible filter toolbars so Classes, Resources, dashboards, and course detail views read as instructional workspaces rather than generic flat panels.
- Migrated from deprecated `middleware.ts` to Next.js 16's `proxy.ts` convention, updating all references and comments across the codebase.
- Updated environment configuration in `env.example` to document `NEXT_PUBLIC_SUPABASE_WHITEBOARD_BUCKET`.
- Upgraded npm dependencies to compatible non-breaking releases (Next.js 16.2.12, React 19.2.8, Better Auth 1.6.25, Supabase JS 2.110.8, TanStack Query 5.101.4, Vitest 4.1.10).
- Changelog entries will be tracked here before the next tagged release.
- Refined the primary UI theme with a lighter blue accent, semantic control/container radii, a simplified brand mark, and the Inter variable interface font.
- Rebuilt the public landing page into a responsive education-focused product overview with proportional typography, a representative UpClass workspace preview, concise feature sections, semantic buttons, and a multi-column footer with live status.
- Rebuilt the app SEO configuration around the production domain `https://upclass.xyz` with richer metadata, structured data, sitemap and robots support, social preview images, and `noindex` protection for authenticated app routes.
- Deploy builds now gate on lint, type-check, and test before migrations and the production build.
- Deploy builds now skip `db:migrate` when `DATABASE_URL` is missing instead of failing before the application build starts.
- Production builds now use webpack instead of Turbopack to avoid Excalidraw CSS build crashes in restricted deploy environments.
- Whiteboard snapshot persistence now uses optimistic concurrency with reload-on-conflict handling instead of last-write-wins version bumps.
- Cache cleanup removed the old page HTML cache layer in favor of the typed IndexedDB data caches already used by offline flows.
- Notification delivery now runs through a shared provider abstraction instead of leaving email and push TODOs in server actions.
- Transactional email delivery now runs through Resend using React Email templates and a dedicated authenticated test endpoint.
- Migrated file storage from UploadThing to Supabase Storage. All uploads (profile images, resources, message media, submission attachments) now go through a unified `/api/upload` route backed by Supabase Storage buckets (`avatars`, `resources`, `media`). Removed the `UPLOADTHING_TOKEN` env var; added `SUPABASE_SERVICE_ROLE_KEY`.

### Fixed
- Invite links shared as `/org/join?token=…` now prefill the invite code instead of requiring the recipient to copy it out of the URL, and land on the join step directly.
- The sidebar workspace switcher's "create or join" action pointed at `/onboard`, a route with no page; it now goes to `/org`.
- Invite links are built from a server-resolved request origin, so they render identically during SSR and hydration.
- Organization admins can no longer remove or re-role other administrators; that stays with the owner.
- Tenant-scoped redirects and links in direct/class messaging, profile, settings, and resource-owner navigation now retain the active organization.
- The migrated shell once again loads recent classes from the active organization instead of leaving the sidebar section empty.
- Notifications no longer run concurrent page/sidebar subscriptions that could apply one realtime unread event twice.
- Public-route matching no longer lets the root entry classify every URL as public; tenant deep links now pass through authentication and organization membership validation.

### Removed
- The standalone Home and Activity sidebar destinations (their routes now redirect to the dashboard) and the sidebar Notifications section, superseded by the header notification bell.
- The favorites feature, `/favorites` route, favorite toggles, and supporting saved-item schema/helpers were removed in favor of a simpler recent-classes sidebar flow.
- Deprecated `middleware.ts` file in favor of Next.js 16 `proxy.ts` convention.
- Unused legacy Supabase middleware helper (`lib/middleware.ts`) that was not imported anywhere.

## [0.1.0] - 2026-03-21

### Added
- Initial Next.js 16 application setup with Drizzle ORM, Better Auth, Google OAuth, Tailwind CSS, and Supabase integration.
- Role-aware onboarding, profile editing, avatar cropping, settings management, and authenticated landing-page routing.
- Core classroom workflows for classes, schedules, announcements, people management, resources, classwork, quizzes, submissions, grading, and reactions.
- Public access paths for class details and resources while preserving authenticated role checks for member actions.
- Home dashboard, activity timeline, messaging, notifications, and responsive navigation for desktop and mobile layouts.
- Whiteboard collaboration support, later rebuilt around Excalidraw with in-app syncing and persisted board documents.
- Progressive Web App support including a service worker, offline indicators, queued offline actions, background caching, and app install controls.
- Shared Zustand stores for user, class, message, notification, and resource state.
- Vitest and React Testing Library setup with initial frontend unit and component coverage.
- Repository guidance and project skill metadata for local coding-agent workflows.

### Changed
- Reworked the marketing landing page, authenticated shell, sidebar, headers, dialogs, and button primitives for a more polished responsive UI.
- Split class UI flows, quiz-taking paths, and shared hooks/types into clearer top-level modules to simplify maintenance.
- Refined loading states across class tabs, home, messages, notifications, and settings with more accurate skeleton UIs.
- Improved class tab transitions with optimistic tab switching, tab-scoped loading, and warm in-session revisits.
- Streamed main authenticated routes through localized Suspense boundaries and consolidated shared server-side loaders.
- Reduced client-side hydration and app chrome across the shell, classes, and messaging surfaces to improve perceived performance.
- Expanded offline and PWA behavior with stronger cache handling, server-status feedback, and app controls.

### Fixed
- Corrected multiple hydration mismatches affecting the home greeting, activity heatmap, and offline-aware home flows.
- Hardened whiteboard session handling and removed legacy whiteboard modules during the Excalidraw migration.
- Restored dynamic browser titles and fixed build blockers related to prerendered routes and remote font loading.
- Stabilized deploy-time database migrations for Vercel builds by updating the migration command and Drizzle tooling.
- Fixed cache-writing behavior in the service worker and disabled zero-interval background refresh polling.
- Resolved dependency alignment issues around React and whiteboard peer requirements.
